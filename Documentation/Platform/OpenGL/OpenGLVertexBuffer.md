## OpenGLVertexBuffer

# Public Functions:
**OpenGLVertexBuffer(float* vertices, uin32_t size)**
-Create array buffer
-Bind array buffer
-Assign buffer data

**~OpenGLVertexBuffer()**
-Delete the buffer

**Bind()**
-Bind the array buffer

**Unbind()**
-Bind the array buffer to 0